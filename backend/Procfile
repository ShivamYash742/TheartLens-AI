# Railway Procfile — defines process types for Railway deployment

# Main FastAPI web server
web: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2

# Celery background worker (add as a separate Railway service)
worker: celery -A app.workers.celery_app worker --loglevel=info --concurrency=2
